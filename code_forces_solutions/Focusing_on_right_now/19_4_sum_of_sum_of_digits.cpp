#include <iostream>
#include <vector>
#include <bits/stl_pair.h>
using namespace std;

typedef long long ll ;



vector<ll> dp(2);

int main(){
    cin.tie(0)->sync_with_stdio(0);
    ll t,n; cin >> t;
    while(t--){

    }
}